
//imports
import Logo from'./logo';
import NextLink from'next/link';import{Container,Box,Link,Stack,Heading,Flex,Menu,MenuItem,MenuList,MenuButton,IconButton,useColorModeValue}from'@chakra-ui/react'as chakraui;import{HamburgerIcon}from'@chakra-ui/icons';
import ThemeToggleButton from'./theme-toggle-button';

abstract public interface navbar {
    public Link navbar{
        //Create the objects to use the LinkItem tag
        navbar LinkItem = new navbar(href, path, children);
        navbar active = new path(href);
        //Create the navbar
        var inactiveColor = chakraui.useColorModeValue('gray200', 'whiteAlpha.900');
        //Return the navbar
        return(
        next NextLink = new href(href);
        chakraui Link = new Link(p=2, bg=active("glassTeal", undefined, color=active("#202023", inactiveColor); //Define the navigation bar
            NextLink(
                Link(
                    children;
                )
            )
        )
    }
}